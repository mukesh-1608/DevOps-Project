# Final Corrected deployment.yaml

apiVersion: apps/v1
kind: Deployment
metadata:
  name: fade-away-chat-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fade-away-chat
  template:
    metadata:
      labels:
        app: fade-away-chat
    spec:
      containers:
      - name: fade-away-chat-container
        # Pull the latest image from Docker Hub
        image: kisakitetta/fadeawaydevops:latest
        # This policy ensures Kubernetes always checks for a new image
        imagePullPolicy: Always
        ports:
        - containerPort: 5000